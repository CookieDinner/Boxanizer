CREATE TABLE ItemTag (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    itemId INTEGER NOT NULL,
    name TEXT NOT NULL
);

selectByItemId:
SELECT * FROM ItemTag WHERE itemId == :itemId;

selectByName:
SELECT * FROM ItemTag WHERE name == :namel;